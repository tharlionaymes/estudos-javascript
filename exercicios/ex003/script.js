let velCarro = 30;

if (40 < velCarro <=  80){
    console.log(`${velCarro} km/h Velocidade Permitida!!!`);
}

else if (velCarro > 80){
    console.log(`${velCarro} km/h Velocidade Acima do Limite Permitido!!!`);
} 

else if (velCarro <= 40 ){
    console.log(`${velCarro} km/h Velocidade Abaixo do Limite Permitido!!!`);
} 
